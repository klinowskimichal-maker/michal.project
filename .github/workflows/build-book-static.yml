name: Zbuduj statyczną Księgę

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Informacja
        run: echo "Automatyczny generator statycznej Księgi jest wyłączony. Publiczna 34-stronicowa Księga korzysta z aktualnego widoku Canva."
